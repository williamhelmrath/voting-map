import React from "react";

const About = () => {
    return (
        <div>
            <h1>"About" page</h1>
            {/* include: project purpose, APIs and resources used, environments used */}
        </div>
    );
};

export default About;